DISCORD_BOT_TOKEN=tu_token_aqui
DISCORD_CLIENT_ID=tu_client_id_aqui
DISCORD_CLIENT_SECRET=tu_client_secret_aqui

# Portal web: login con Discord (usa mismo Client ID/Secret de la app en Discord Developer Portal)
# SESSION_SECRET=opcional_cambiar_en_produccion

# SQLite (por defecto; migrable a PostgreSQL)
# DATABASE_PATH=./data/bot.db

# Moneda (CLP por defecto, extensible para futuro módulo finanzas)
CURRENCY_CODE=CLP
CURRENCY_SYMBOL=$
CURRENCY_LOCALE=es-CL

# Vista web (lista en el celular). /ver-lista genera un link que usa esta URL.
# Local: http://localhost:3000  |  EC2: http://TU_IP_PUBLICA:3000
# WEB_PORT=3000
# WEB_BASE_URL=http://localhost:3000
